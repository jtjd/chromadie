# Cursor trail curation — 2026-09-08

## Plan and acceptance

Reduce the default chooser from 23 to 16 distinct effects. Keep Glyph Bloom,
Joy Burst and Bubble Lift as the visual benchmarks. Replace generic lines and
square particles with authored pixel fragments, prismatic facets, rising embers,
ink splashes, orbiting stars, rain ripples, gold confetti, color stamps and
expanding eclipse rings. Keep the existing plasma, text and elastic behaviors.

Consolidate Signal Trace, Chroma Ribbon, Comet Thread, Static Echo, Ghost Tail,
Marker Stroke and Orbit Dot into compatible retained renderers. Preserve all
23 IDs, inventory and equipped values; show a consolidated item in the editor
when already selected so existing profiles can change it without losing state.
This is presentation curation, with no schema migration or entitlement change.
The server catalog and historical progression rewards remain intact.

Fix idle expiration, pointer exit, event-rate-dependent emission, reduced-motion
handling and hidden-document animation. Verify the shared renderer in desktop
and compact previews, pointer and reduced-motion modes. Add regression tests,
run the full required validation suite and record any failed gates.
