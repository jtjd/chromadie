// Served only by the local browser test; use Vite's shared Svelte runtime.
export { mount, unmount } from 'svelte';
export { default as Trail } from '../../src/lib/cursor-trail/CursorTrailLayer.svelte';
export { default as Environment } from '../../src/lib/ProfileEnvironmentLayer.svelte';
